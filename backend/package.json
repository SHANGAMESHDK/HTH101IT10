{
  "name": "smart-parking-backend",
  "version": "1.0.0",
  "description": "VAYGO Smart Parking Backend — Express + WebSocket + Razorpay + Google Auth",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --ignore data/ --ignore uploads/ server.js"
  },
  "dependencies": {
    "@google/genai": "^2.24.0",
    "cors": "^2.8.5",
    "dotenv": "^18.0.3",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "razorpay": "^2.9.8",
    "ws": "^8.16.0"
  },
  "optionalDependencies": {
    "serialport": "^13.0.0",
    "@serialport/parser-readline": "^12.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
